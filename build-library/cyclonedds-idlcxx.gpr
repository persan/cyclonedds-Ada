with "cyclonedds.gpr";
with "cyclonedds-idl.gpr";
library project CycloneDDS.idl is

   for Source_Dirs use ();
   for Externally_Built use "True";
   for Library_Name use "idlcxx";
   for Library_Kind use "dynamic";
   for Library_Dir use "../../lib64";

end CycloneDDS.idl;
